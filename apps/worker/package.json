{
  "name": "@officepulse/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/worker.ts",
    "build": "bun build src/worker.ts --outdir dist --target bun"
  }
}

